[tool.poetry]
name = "cad-tests"
version = "0.1.0"
description = "DXF parsing pipeline test"
authors = ["Davide <davide@example.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
ezdxf = "^1.4.4"

[tool.poetry.dev-dependencies]
pytest = "^7.4.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
